---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "truenas_user Resource - truenas"
subcategory: ""
description: |-
  Manages a TrueNAS local user.
  ~> Note: The password attribute is write-only and cannot be read back from TrueNAS. After terraform import, password will be null.
---

# truenas_user (Resource)

Manages a TrueNAS local user.

~> **Note:** The `password` attribute is write-only and cannot be read back from TrueNAS. After `terraform import`, `password` will be null.

## Example Usage

```terraform
resource "truenas_group" "media" {
  name = "media"
  smb  = true
}

resource "truenas_user" "john" {
  username  = "john"
  full_name = "John Doe"
  group     = truenas_group.media.id
  smb       = true
  home      = "/mnt/tank/home/john"
  shell     = "/usr/bin/bash"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `full_name` (String) The full name of the user.
- `username` (String) The username.

### Optional

- `email` (String) The email address of the user.
- `group` (Number) The primary group ID. If not specified and group_create is true, a group with the username is created.
- `group_create` (Boolean) Create a new primary group with the same name as the user. Only used during creation.
- `groups` (List of Number) List of auxiliary group IDs.
- `home` (String) The home directory path.
- `home_create` (Boolean) Create the home directory if it doesn't exist. Only used during creation.
- `locked` (Boolean) Whether the user account is locked.
- `password` (String, Sensitive) The user password. Write-only — cannot be read back from TrueNAS.
- `password_disabled` (Boolean) Whether password login is disabled for this user.
- `shell` (String) The user's login shell (e.g. /usr/bin/bash, /usr/sbin/nologin).
- `smb` (Boolean) Whether the user is available for SMB authentication.
- `sshpubkey` (String) SSH public key for the user.

### Read-Only

- `builtin` (Boolean) Whether this is a built-in system user.
- `id` (Number) The unique identifier of the user.
- `uid` (Number) The UID of the user.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import truenas_user.example 42
```
